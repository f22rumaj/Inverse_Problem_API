[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "iproblem"
version = "1.0.0"
description = "Bayesian Inference API for Nanomechanical Spectrometry"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "tqdm",
    "psutil"
]
